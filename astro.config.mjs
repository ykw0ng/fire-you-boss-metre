// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// Update `site` to your final domain once deployed.
export default defineConfig({
  site: 'https://fireyourbossmetre.com',
});
